# Coding Agent 项目计划

> 版本：v0.1 · 2026-08-27 · 截止：2026-09-02 24:00（北京时间）

## 1. 目标

实现一个终端编程智能体：模型自主读写本地项目、执行命令、根据反馈继续工作，直到完成任务或触发明确的停止条件。

项目追求“小内核、强工程性”：核心 Agent 机制自行实现，通用基础能力复用成熟库；最终成果必须可运行、可测试、可追踪、可解释。

## 2. 合规边界

- **自行实现**：Agent 循环、上下文策略、工具定义与调度、模型输出映射、终止条件、权限决策、重试与错误语义。
- **允许复用**：模型 HTTP 客户端、数据校验、CLI/终端渲染、进程管理、`.gitignore` 匹配和测试框架。
- **不使用**：LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等 Agent 框架；服务端代码执行或文件工具；现成 Coding Agent 的封装。
- 对处于灰区、会隐藏核心逻辑的高级抽象库（如通用 Agent/Tool 编排层）默认不用。

## 3. 交付范围

### P0：核心闭环

- 单次任务与交互式 CLI。
- OpenAI-compatible 模型适配器，以及用于测试的 `FakeModel`。
- 工具：`list_files`、`read_file`、`search_text`、`write_file`、`replace_text`、`run_command`。
- 手写模型—工具—观察循环；工具失败作为结构化观察返回模型。
- 工作区路径隔离、命令超时、输出截断、最大步数、重复调用检测和用户中断。
- 上下文预算：保留任务与近期动作，压缩旧工具输出，避免上下文无限增长。

### P1：承诺完成的增强能力

- 流式终端反馈与结构化任务进度。
- 结构化任务计划：维护待办、进行中和已完成状态，但不记录模型隐式思维过程。
- Verification Gate：最后一次写操作之后没有成功测试、构建或检查证据时，只能报告“完成但未验证”。
- `safe` / `auto` 两档权限模式；危险操作始终拒绝或确认。
- 会话持久化与恢复。
- 写文件前生成 Diff，并支持本次会话内撤销。
- JSONL 事件轨迹与 `inspect <run-id>`：记录工具调用、结果、耗时、错误和停止原因。
- 自动评测：单文件修复、跨文件修改、新增功能、间接故障调试四类任务，统计成功率、步数、耗时和工具错误。

### P2：时间允许再做

- 第二种模型协议适配器。
- 只读工具调用的安全并发。
- Python 符号/AST 摘要工具。

### 非目标

Web UI、多 Agent、向量数据库、MCP、插件市场、远程沙箱和 IDE 集成不进入本次范围。

## 4. 架构

```text
CLI
 └─ AgentRunner（状态机与循环）
     ├─ ModelAdapter（供应商协议隔离）
     ├─ ContextManager（预算与压缩）
     ├─ Stop / Verification Policy
     ├─ ToolRegistry（校验与执行）
     │   ├─ File/Search Tools
     │   └─ Command Tool
     ├─ Policy（路径、权限、资源限制）
     ├─ SessionStore（保存与恢复）
     └─ EventRecorder（轨迹与指标）
```

内部边界采用少量稳定接口：`ModelAdapter.complete()`、`Tool.execute()`、`Policy.authorize()`、`EventSink.emit()`。事件轨迹是事实记录，上下文是从事实中选择出的模型视图，两者分离。

```text
CREATED → PLANNING → ACTING ↔ OBSERVING → VERIFYING
                                      ├→ COMPLETED
                                      ├→ COMPLETED_UNVERIFIED
                                      └→ FAILED
```

工具调用默认顺序执行，因为文件修改具有顺序依赖；只有证明无副作用的只读工具才考虑并发。

## 5. 依赖策略

| 依赖 | 用途 | 不由它负责的内容 |
|---|---|---|
| `openai` | 模型请求与原生 tool calling 传输 | Agent 循环、重试决策、上下文 |
| `pydantic` | 配置、工具参数和事件校验 | 工具调度与业务规则 |
| `typer`、`rich`、`prompt-toolkit` | CLI、流式展示、交互历史 | Agent 状态机 |
| `pathspec` | 尊重 `.gitignore` | 工作区安全边界 |
| `psutil` | 超时后终止进程树 | 命令授权策略 |
| `pytest` | 单元、集成与评测测试 | 运行时逻辑 |

依赖统一由 `uv` 管理并锁定版本；API key 仅通过环境变量或未入库配置提供。

## 6. 里程碑与提交

| 日期 | 里程碑 | 主要验收 |
|---|---|---|
| 8/27 | 规划与骨架 | 依赖、目录、CLI、测试可运行 |
| 8/28 | 模型与核心循环 | `FakeModel` 驱动多轮工具调用 |
| 8/29 | 工具与安全 | 在临时项目中读写、搜索、运行测试 |
| 8/30 | 上下文与验证 | 计划、压缩、停止、Verification Gate 测试通过 |
| 8/31 | Diff、撤销与轨迹 | 修改可审查，运行过程可复盘 |
| 9/1 | 评测与稳定性 | 四类真实任务完成，形成指标报告 |
| 9/2 | 演示与冻结 | 文档、视频、安全扫描；截止后不再推送 |

每个里程碑独立提交并保留历史，不 squash、不改写已推送提交。

## 7. 完成标准

- Agent 能在隔离的示例仓库中发现失败测试、定位代码、修改并验证全部测试通过。
- 最终状态区分“已验证完成”和“完成但未验证”，模型的口头声明不能替代外部验证证据。
- 无 API 的单元测试覆盖循环、工具、权限、停止和上下文；真实 API 测试通过显式开关运行。
- 路径穿越、重复工具调用、命令超时、超长输出和模型暂时失败均有确定行为。
- 仓库 `README.md` 讲清运行与设计；提交用 `README.txt` 控制在 1000 汉字内。
- 两分钟视频展示一个完整闭环，面试时能解释每项核心设计的替代方案与取舍。
